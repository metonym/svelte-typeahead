{
  "private": true,
  "scripts": {
    "dev": "svite",
    "build": "svite build --base= --assetsDir=assets",
    "deploy": "npx gh-pages -d dist"
  },
  "devDependencies": {
    "mdsvex": "^0.8.8",
    "svelte": "^3.27.0",
    "svelte-hmr": "^0.11.0",
    "svite": "^0.7.1",
    "svelte-typeahead": "0.1.1"
  }
}
